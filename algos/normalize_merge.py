# algos/normalize_merge.py
def derive_lovs(materials_master, hardware_master):
    # Return UI lists/dicts; placeholders for now
    materials_lov = []
    thickness_lov = {}
    return materials_lov, thickness_lov

def run(materials_master, operations_master, hardware_master, outsideprocess_master,
        rates_master, cleaned_dataset, ops_overlay, hardware_overlay,
        outside_process_overlay, settings_overlay, rate_overlay_cycle):
    # Normalize inputs; no-op stub
    return cleaned_dataset
